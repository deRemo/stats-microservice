# -*- coding: utf-8 -*-
from setuptools import find_packages, setup

setup(name='stats',
      version='0.1',
      url='',
      description='statistics for Dice-with-microservices',
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False)
